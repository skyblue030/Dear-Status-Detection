# LINE Notify access token used by capture_process to send alerts on Aggressive state.
# Note: LINE Notify service was discontinued by LINE on 2025-03-31.
# This var is kept so the existing code still has somewhere to read from; consider
# replacing the notification mechanism (e.g. with LINE Messaging API or Discord webhook).
LINE_NOTIFY_TOKEN=your_token_here
